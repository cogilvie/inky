# A library for interfacing with various pimoroni eink displays

## Supported displays
* WHat
* Impression 7.3

## Dependencies

```sh
$ sudo apt install pkg-config libfreetype6-dev libfontconfig1-dev
```